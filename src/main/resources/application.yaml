spring:
  application:
    name: taco-cloud

  # thymeleaf
  thymeleaf:
    cache: false

# БД
  data:
    mongodb:
      port: 27017
      database: tacoclouddb
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.5
